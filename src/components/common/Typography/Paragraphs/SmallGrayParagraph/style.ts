import styled from 'styled-components';
import SmallParagraph from '@/components/common/Typography/Paragraphs/SmallParagraph';

const StyledSmallGrayParagraph = styled(SmallParagraph)`
  && {
    color: var(--secondaryBlue);
  }
`;

export default StyledSmallGrayParagraph;
