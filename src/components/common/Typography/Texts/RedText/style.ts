import styled from 'styled-components';
import BaseText from '@/components/common/Typography/Texts/BaseText';

const StyledRedText = styled(BaseText)`
  color: var(--brightRedColor);
`;

export default StyledRedText;
