import styled from 'styled-components';
import BaseText from '@/components/common/Typography/Texts/BaseText';

const StyledGreenText = styled(BaseText)`
  color: var(--primaryGreen);
`;

export default StyledGreenText;
