import { Collapse } from 'antd';
import styled from 'styled-components';

const StyledBaseCollapse = styled(Collapse)`
  .ant-collapse-header-text {
    font-family: var(--notoSans);
  }
`;

export default StyledBaseCollapse;
