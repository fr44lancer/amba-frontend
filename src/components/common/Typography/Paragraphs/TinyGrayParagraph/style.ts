import styled from 'styled-components';
import TinyParagraph from '@/components/common/Typography/Paragraphs/TinyParagraph';

const StyledTinyGrayParagraph = styled(TinyParagraph)`
  && {
    color: var(--secondaryBlue);
  }
`;

export default StyledTinyGrayParagraph;
